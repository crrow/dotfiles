{
  "name": "dotfiles",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bun run src/setup.ts",
    "setup:dry": "bun run src/setup.ts --dry-run",
    "test:vm": "bun run test/harness.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
