# Tools managed by mise. Run `mise install` after cloning.
# This is the project-local toolchain; user-level tools live in
# ~/.config/mise/config.toml (deployed by setup.ts).

[tools]
bun = "latest"

[settings]
# Trust this config without prompting on `mise install`.
experimental = true
